[package]
name = "arb-reconciliation"
version = "0.1.0"
edition.workspace = true

[features]
default = []

[dependencies]
arb-domain = { path = "../arb-domain" }
arb-ledger = { path = "../arb-ledger" }
